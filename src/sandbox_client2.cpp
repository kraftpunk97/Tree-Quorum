#include <iostream>
#include <string>
#include <array>
#include "include/client.hpp"

int main(int argv, char** argc) {
    std::array<std::string, 2> host;
    for (int i=0; i<2; i++)
        host[i] = "localhost";
    std::array<int, 2> port;
    Client C2();
    return 0;
}